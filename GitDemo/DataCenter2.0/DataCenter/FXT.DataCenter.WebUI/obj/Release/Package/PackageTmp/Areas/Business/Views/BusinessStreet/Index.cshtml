@using Webdiyer.WebControls.Mvc
@using FXT.DataCenter.Domain.Models
@{
    ViewBag.Title = "商业街";
}
@model PagedList<Dat_Project_Biz>
@section StyleSheet{
    <style type="text/css">
        select
        {
            margin: 1px;
            width: 150px;
        }

        .p5
        {
            padding: 0px;
            padding-top: 5px;
            line-height: 200%;
            background: #fbfbfb;
        }

            .p5 label
            {
                width: 100px;
            }

            .p5 select
            {
                width: 133px;
            }

        #hourslider
        {
            display: inline-block;
            width: 120px;
            margin-top: 10px;
            margin-left: 30px;
        }

        .scrollBar
        {
            width: 100%;
            overflow: scroll;
            overflow-x: scroll;
            overflow-y: hidden;
        }

        .import
        {
            display: none;
        }

        .selfDefineExport
        {
            display: none;
        }
    </style>
}
@section Javascript{
    <script type="text/javascript">
        $(document).ready(function () {

            //面包屑导航
            $("#navigation .breadcrumb li:eq(1)").remove();
            $("#navigation .breadcrumb li:eq(2)").remove();
            var html = " <li><span><a>商业街</a></span></li>";
            $("ul.breadcrumb").append(html);
            
            //导出
            $("#btnExport").bind("click", function () {
                $("#search").attr("action", "/Business/BusinessStreet/Export");
            });

            //批量导入
            $("#btnBatchImport").on("click", function () {
                var $import = $(".import");
                var length = $import.length;
                if (length > 0) {
                    $import.removeClass("import").addClass("importFlag");
                } else {
                    $(".importFlag").removeClass("importFlag").addClass("import");
                }
            });

            //自定义导出
            $("#btnSelfDefineExport").on("click", function () {
                var $import = $(".selfDefineExport");
                var length = $import.length;
                if (length > 0) {
                    $import.removeClass("selfDefineExport").addClass("selfDefineExportFlag");
                } else {
                    $(".selfDefineExportFlag").removeClass("selfDefineExportFlag").addClass("selfDefineExport");
                }
            });

            //删除
            $("#delete").unbind();
            $("#delete").click(function () {
                var ids = [];
                var checks = $("input[name='ids']:checked");
                if (checks.length == 0) {
                    alert("请选择要删除的数据");
                    return;
                }
                if (confirm("是否确定删除？")) {
                    checks.each(function () {
                        ids.push($(this).val());
                    });
                    $.ajax({
                        type: "POST",
                        url: "@Url.Action("Delete")",
                        data: { ids: ids },
                        traditional: true,
                        success: function (data) {
                            if (data.result) {
                                if (data.msg != "") {
                                    alert(data.msg);
                                }
                                location.reload();
                            } else {
                                alert(data.msg);
                            }
                        }
                    });
                }

            });
        });

        function view(obj, projectId) {
            $.get('@Url.Action("BusinessBuildingCount")', { projectId: projectId }, function (data) {
                $(obj).html('<i class="icon-eye-close"></i>&nbsp;' + data);
                $(obj).attr("href", "/Business/BusinessBuild/Index?ProjectId=" + projectId);
            });
        }
    </script>
    @{Html.RegisterMvcPagerScriptResource();}
}

<div class="row-fluid" id="condi">

    @using (Html.BeginForm("Index", "BusinessStreet", null, FormMethod.Get, new { id = "search", @class = "form-inline" }))
    { 
        <table style="margin-bottom: 10px; line-height: 35px; width: 75%;">
            <tr>
                <td>行政区:
                </td>
                <td>@Html.DropDownList("areaid")
                </td>
                <td>商业街:
                </td>
                <td>@Html.TextBox("projectName", Request["projectName"], new { @class = "m-wrap small" })
                </td>
            </tr>
            <tr>
                <td colspan="6">
                    <a class="btn blue" title='新增' href="@Url.Action("Create")">
                        <i class="icon-plus icon-white"></i>新增</a> <a class="btn red" id="delete" href="javascript:;">
                            <i class="icon-trash icon-white"></i>删除</a>
                    &nbsp; &nbsp; &nbsp; &nbsp;
                    <button id="btnRetrive" class="btn">
                        查 询 <i class="icon-search"></i>
                    </button>
                    <a class="btn" href="@Url.Action("UploadFile")">导入</a>
                    <button id="btnExport" class="btn" name="btnExport">
                        导 出
                    </button>
                    <a class = "btn" href="@Url.Action("Index", "BusinessCircle")" target="_blank"><i class="icon-share-alt">&nbsp;转到商圈</i></a>
                </td>
            </tr>
            <tr>
                <td colspan="6">
                    <a id="btnBatchImport" class="btn">批量导入</a>
                    <a id="btnSelfDefineExport" class="btn">自定义导出</a>
                </td>
            </tr>
            <tr>
                <td colspan="6">
                    <a title="Excel导入" style="margin:2px;" class = "btn import" href="@Url.Action("UploadFile", "BusinessStreet")">商业街导入</a>
                    <a title="Excel导入" style="margin:2px;" class = "btn import" href="@Url.Action("UploadFile", "BusinessBuild")">商业楼栋导入</a>
                    <a title="Excel导入" style="margin:2px;" class = "btn import" href="@Url.Action("UploadFile", "BusinessFloor")">商业楼层导入</a>
                    <a title="Excel导入" style="margin:2px;" class = "btn import" href="@Url.Action("UploadFile", "BusinessHouse")">商业房号导入</a>
                    <a title="Excel导入" style="margin:2px;" class = "btn import" href="@Url.Action("UploadFile", "BusinessStore")">商铺导入</a>
                </td>
            </tr>
            <tr>
                <td colspan="6">
                    <a title="自定义导出" style="margin:2px;" class = "btn selfDefineExport" href="@Url.Action("SelfDefineExport", "BusinessStreet")">商业街导出</a>
                    <a title="自定义导出" style="margin:2px;" class = "btn selfDefineExport" href="@Url.Action("SelfDefineExport", "BusinessBuild")">商业楼栋导出</a>
                    <a title="自定义导出" style="margin:2px;" class = "btn selfDefineExport" href="@Url.Action("SelfDefineExport", "BusinessHouse")">商业房号导出</a>
                </td>
            </tr>
        </table>
    }
</div>

<table class="table table-striped table-hover ">
    <thead>
        <tr>
            <th style="width: 8px;">
                <input type="checkbox" id="checkall" class="group-checkable" />
            </th>
            <th>商业街
            </th>
            <th>所属行政区
            </th>
            <th>所属商圈
            </th>
            <th>与商圈的关联度
            </th>
            <th>别名
            </th>
            <th>地址
            </th>
            <th>开业时间
            </th>
            <th>商业楼栋
            </th>
            <th style="width: 59px;">操作
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var m in Model)
        {
            var chValue = m.FxtCompanyId + "#" + m.ProjectId;
            <tr>
                <td>
                    <input type="checkbox" class="checkboxes" name='ids' value='@chValue' />
                </td>
                <td>
                    <a href="@Url.Action("Edit", new { id = chValue })">@m.ProjectName </a>
                </td>
                <td>
                    @m.AreaName
                </td>
                <td>
                    @m.SubAreaName
                </td>
                <td>
                    @m.CorrelationTypeName
                </td>
                <td>
                    @m.OtherName
                </td>
                <td>
                    @m.Address
                </td>
                <td>
                    @m.OpenDate
                </td>
                <td>
                    <a class="btn mini btn-xs blue" onclick ="javascript:view(this,'@m.ProjectId');"><i class=" icon-eye-open"></i>查看</a>
                </td>

                <td>
                    <a class="btn mini purple" title='编辑' href="@Url.Action("Edit", new { id = chValue })">
                        <i class="icon-edit"></i>编辑 </a>
                </td>
            </tr>
        }
    </tbody>
</table>


<div class="pager">
    @{
        var totalPageCount = Model == null ? 0 : Model.TotalPageCount;
        var totalItemCount = Model == null ? 0 : Model.TotalItemCount;
        var currentPageIndex = Model == null ? 0 : Model.CurrentPageIndex;
    }
    <div>共 @totalPageCount 页 @totalItemCount 条记录,当前为第 @currentPageIndex 页</div>
    @if (Model != null && Model.Any())
    {
        @Html.Pager(Model, new PagerOptions { AutoHide = false, ShowPageIndexBox = true, PageIndexBoxType = PageIndexBoxType.TextBox })
    }

</div>

