@{
    ViewBag.Title = "楼盘附属房价格";
}
@using FXT.DataCenter.Domain.Models;
@using Webdiyer.WebControls.Mvc
@model PagedList<DAT_SubHousePrice>
           @section StyleSheet{
    <style type="text/css">
        select
        {
            margin: 1px;
            width: 150px;
        }
        .p5
        {
            padding: 0px;
            padding-top: 5px;
            line-height: 200%;
            background: #fbfbfb;
        }
        .p5 label
        {
            width: 100px;
        }
        .p5 select
        {
            width: 133px;
        }
        #hourslider
        {
            display: inline-block;
            width: 120px;
            margin-top: 10px;
            margin-left: 30px;
        }
        .scrollBar
        {
            width: 100%;
            overflow: scroll;
            overflow-x: scroll;
            overflow-y: hidden;
        }

    </style>
}
@section Javascript{
 <script type="text/javascript">

     $(function () {
         var locationHref = window.location.href, projectName = "@(ViewBag.projectName)";
         if (locationHref.indexOf("House/Project/GetProjectPrice")) {
             $("#navigation .breadcrumb li:eq(1)").remove();
             $("#navigation .breadcrumb li:eq(2)").remove();
             var html = " <li><span><a href=\"/House/Project/Index\">住宅基础数据</a></span> <i class=\"icon-angle-right\"></i></li>";
             if (projectName) {
                 html += "<li><span><a href=\"/House/Project/Edity?projectid=" + encodeURIComponent("@ViewBag.projectPara") + "\">" + projectName + "</span></a><i class=\"icon-angle-right\"></i></li>";
              }
             html += "<li><span>附属房屋价格</span></li>";
             $("ul.breadcrumb").append(html);
         }
     });
 </script>
}
@*@using (Html.BeginForm("Delete", "Land", FormMethod.Post, new { id = "mainForm" }))
{
   *@
    <div class="scrollBar">
        <table class="table table-striped table-hover ">
            <thead>
                <tr>
                    @* <th style="width: 8px;">
                        <input type="checkbox" id="checkall" class="group-checkable" />
                    </th>*@
                    <th>
                        附属房类型
                    </th>
                    <th>
                        单价(元/㎡)
                    </th>
                    <th>
                        操作
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var m in Model)
                {
                    //var chValue = m.fxtcompanyid + "#" + m.projectid;
           
                    <tr>
                        @*  <td>
                            <input type="checkbox" class="checkboxes" name='ids' value='@chValue' />
                        </td>*@
                        <td>@m.CodeName
                        </td>
                        <td>@m.subhouseunitprice
                        </td>
                        <td>
                            <a class="btn mini purple thickbox" title='编辑楼盘项目配套' href="@Url.Action("EdityProjectPrice", new { projectid = ViewBag.projectid, codename = Server.UrlEncode(m.CodeName) })TB_iframe=true&height=350&width=520">
                                <i class="icon-edit"></i>编辑</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
@*}*@
<div class="pager">
    <div style="float: left;">共 @Model.TotalPageCount 页 @Model.TotalItemCount 条记录，当前为第 @Model.CurrentPageIndex 页</div>
    @Html.Pager(Model, new PagerOptions { AutoHide = false })
</div>

