@using FXT.DataCenter.Domain.Models;
@using Webdiyer.WebControls.Mvc
@model PagedList<DAT_Land>        
@{
    ViewBag.Title = "土地基础数据";
}
@section StyleSheet{
    <style type="text/css">
        select
        {
            margin: 1px;
            width: 150px;
        }

        .p5
        {
            padding: 0px;
            padding-top: 5px;
            line-height: 200%;
            background: #fbfbfb;
        }

        .p5 label
        {
            width: 100px;
        }

        .p5 select
        {
            width: 133px;
        }

        #hourslider
        {
            display: inline-block;
            width: 120px;
            margin-top: 10px;
            margin-left: 30px;
        }

        .scrollBar
        {
            width: 100%;
            overflow: scroll;
            overflow-x: scroll;
            overflow-y: hidden;
        }
        .ellipsis {
            width: 150px;
            display:inline-block;
            text-overflow:ellipsis;
            white-space:nowrap;
            overflow:hidden;
        }
    </style>
}
@section Javascript{
    <script type="text/javascript">
        var UrlDelete = '@Url.Action("Delete")';
        $(document).ready(function () {
            $("#startdate,#startenddate, #enddate").datepicker({ format: 'yyyy-mm-dd' });

            $("#areaid").change(function () {
                var areaId = $(this).find("option:selected").val();
                BindSubArea(areaId);
            });
            $("#btnExport").bind("click", function () {
                $("#search").attr("action", "/Land/Land/ExcelImport");
            });
            $("#btnAll").bind("click", function () {
                var startdate = $("#startdate").val(), enddate = $("#enddate").val();
                if (startdate != null && enddate != null)
                {
                    startdate = startdate.replace(/-/g, "/");
                    enddate = enddate.replace(/-/g, "/");
                    var time_start = new Date(startdate);   
                    var time_end = new Date(enddate);   
                    if(Date.parse(time_start) - Date.parse(time_end)>0)   
                    {   
                        alert("土地使用起始日期的范围有误");
                        return false;
                    } 
                }
                $("#search").submit();
            });
            //删除
            $("#delete").unbind();
            $("#delete").click(function () {
                var ids = [];
                var checks = $("input[name='ids']:checked");
                if (checks.length == 0) {
                    alert("请选择要删除的数据");
                    return;
                }
                if (confirm("是否确定删除？")) {
                    checks.each(function () {
                        ids.push($(this).val());
                    });
                    $.ajax({
                        type: "POST",
                        url: UrlDelete,
                        data: { ids: ids },
                        traditional: true,
                        success: function (data) {
                            if (data.result) {
                                if (data.msg != "") {
                                    alert(data.msg);
                                }
                                location.reload();
                            } else {
                                alert(data.msg);
                            }
                        }
                    });
                }

            });

        });
    </script>
    @{Html.RegisterMvcPagerScriptResource();}
}
<div style="text-align: right;">
    <a style="cursor: pointer;" id="control">收起∧</a>
</div>
<div class="row-fluid" id="condi">
    @using (Html.BeginForm(null, null, null, FormMethod.Get, new { id = "search", @class = "form-inline" }))
    { 
        <table style="width: 100%; margin-bottom: 10px; line-height: 35px;">
            <tr>
                <td>行政区：
                </td>
                <td>@Html.DropDownList("areaid", ViewBag.arealist as List<SelectListItem>)
                </td>
                <td>宗地号：
                </td>
                <td>@Html.TextBox("landno", Request["landno"], new { @class = "m-wrap small", style = "width:138px!important;" })
                </td>
                @*  <td>使用证号:
                </td>
                <td>@Html.TextBox("fieldno", Request["fieldno"], new { @class = "m-wrap small", style = "width:138px!important;" })
                </td>*@
            </tr>
            <tr>
                <td>片区：
                </td>
                <td>@Html.DropDownList("subareaid", ViewBag.subarealist as List<SelectListItem>)
                </td>
                <td>使用起始日期:
                </td>
                <td colspan="3" style="padding-top: 5px;">@Html.TextBox("startdate", null, new {style="width:140px!important;" })
                    到
                @Html.TextBox("enddate", null, new { style="width:140px!important;" })
                </td>
                @*   <td>所有者:
                </td>
                <td>@Html.DropDownList("landownerid", ViewBag.companyList as List<SelectListItem>)
                </td>
                <td>使用者:
                </td>
                <td>@Html.DropDownList("landuseid", ViewBag.companyList as List<SelectListItem>)
                </td>*@
            </tr>
            <tr>
                <td>实际用途：
                </td>
                <td>@Html.DropDownList("planpurpose", ViewBag.landcodelist as List<SelectListItem>)
                </td>



            </tr>
            <tr>
                <td colspan="6">

                    <a class="btn blue thickbox" title='添加土地基础信息'  href="@Url.Action("Edity", new { id = 0+"#"+0 })?TB_iframe=true&height=500&width=860">
                        <i class="icon-plus icon-white"></i>新增 </a><a class="btn red" id="delete" href="javascript:void(0);" style="display:@(Model.Count > 0 ? "" : "none")">
                            <i class="icon-trash icon-white"></i>删除 </a>
                    &nbsp;&nbsp;
                    <button type="button" id="btnAll" name="command" class="btn" value="all">
                        查询 <i class="icon-search"></i>
                    </button>
                    <a id="btnImport" title="Excel导入" class = "btn" href="@Url.Action("UploadFile")">导 入</a>
                    <button id="btnExport" class="btn">
                        导 出
                    </button>
                    @* <button id="btnStatitics" class="btn">
                    统 计
                </button>*@
                </td>
            </tr>
        </table>
    }
</div>
@using (Html.BeginForm("Delete", "Land", FormMethod.Post, new { id = "mainForm" }))
    {
   
    <div class="scrollBar">
        <table class="table table-striped table-hover ">
            <thead>
                <tr>
                    <th style="width: 8px;">
                        <input type="checkbox" id="checkall" class="group-checkable" />
                    </th>
                    <th>城市
                    </th>
                    <th>行政区
                    </th>
                    @* <th>
                        片区
                    </th>*@
                    <th>宗地号
                    </th>
                    @*<th>
                        使用证号
                    </th>*@
                    <th>土地名称
                    </th>
                    <th style="width: 150px;">土地位置
                    </th>
                    @* <th>
                        使用权类型
                    </th>
                    <th>
                        使用权性质
                    </th>*@
                    @*  
                    <th>
                        使用结束日期
                    </th>*@
                    @* <th>
                        使用年限
                    </th>*@
                    <th>规划用途
                    </th>
                    <th>使用起始日期
                    </th>
                    @* <th>
                        面积
                    </th>
                    <th>
                        总建筑面积
                    </th>
                    <th>
                        容积率
                    </th>*@
                    @*  <th>
                        创建时间
                    </th>*@
                     <th style="width: 59px;">操作
                </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var m in Model)
                {
                    var chValue = m.fxtcompanyid + "|" + m.landid;
                    <tr>
                        <td>
                            <input type="checkbox" class="checkboxes" name='ids' value='@chValue' />
                        </td>
                        <td>@m.CityName
                        </td>
                        <td>@m.AreaName
                        </td>
                        @* <td>@m.SubAreaName
                        </td>*@
                        <td>@m.landno
                        </td>
                        @*<td>@m.fieldno
                        </td>*@
                        <td>
                            <a class="thickbox" title='@m.landname' href="@Url.Action("Edity", new { id = m.fxtcompanyid+"#"+m.landid })?TB_iframe=true&height=500&width=860">
                                <i class=""></i>@m.landname</a>
                        </td>
                        <td>
                            <span class="ellipsis">@m.address</span>
                        </td>
                        @*  <td>
                            @m.LandTypeName
                        </td>
                        <td>@m.UserTypeName
                        </td>*@
                        @* 
                        <td>@(m.enddate == null ? "" : Convert.ToDateTime(m.enddate).ToString("yyyy-MM-dd"))
                        </td>*@
                        @* <td>@m.useyear
                        </td>*@
                        <td>@m.planpurpose
                        </td>
                        <td>@(m.startdate == null ? "" : Convert.ToDateTime(m.startdate).ToString("yyyy-MM-dd"))
                        </td>
                        @* <td>@m.landarea
                        </td>
                        <td>@m.buildingarea
                        </td>
                        <td>@m.cubagerate
                        </td>*@
                        @* <td>@(m.createdate == null ? "" : m.createdate.ToString("yyyy-MM-dd"))
                        </td>*@

                          <td>
                        <a class="btn mini purple thickbox" title='编辑' href="@Url.Action("Edity", new { id = m.fxtcompanyid+"#"+m.landid })?TB_iframe=true&height=500&width=900">
                            <i class="icon-edit"></i>编辑 </a>
                    </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
    }

<div class="pager">
    @{
            var totalPageCount = Model == null ? 0 : Model.TotalPageCount;
            var totalItemCount = Model == null ? 0 : Model.TotalItemCount;
            var currentPageIndex = Model == null ? 0 : Model.CurrentPageIndex;
    }
    <div>共 @totalPageCount 页 @totalItemCount 条记录,当前为第 @currentPageIndex 页</div>
    @if (Model != null && Model.Any())
    {
        @Html.Pager(Model, new PagerOptions { AutoHide = false, ShowPageIndexBox = true, PageIndexBoxType = PageIndexBoxType.TextBox })
    }
</div>

<script src="@Url.Content("~/Content/Scripts/cityBind/city.js")" type="text/javascript"></script>
